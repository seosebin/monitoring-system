input {
    file {
        path => "/app/logs/access.log"
        start_position => "beginning"
        sincedb_path => "/dev/null"
        type => "access"
    }

    file {
        path => "/app/logs/error.log"
        start_position => "beginning"
        sincedb_path => "/dev/null"
        type => "error"
    }
}

filter {
}

output {
    stdout {
        codec => rubydebug
    }
}